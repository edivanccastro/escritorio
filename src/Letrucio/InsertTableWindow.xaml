<Window x:Class="Letrucio.InsertTableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Inserir Tabela"
        Width="300" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#F5F5F5">
    <StackPanel Margin="16">
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="80" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="N̼mero de colunas:" VerticalAlignment="Center" />
            <TextBox x:Name="ColumnsBox" Grid.Column="1" Text="3" Height="24" VerticalContentAlignment="Center" />
        </Grid>
        <Grid Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="80" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="N̼mero de linhas:" VerticalAlignment="Center" />
            <TextBox x:Name="RowsBox" Grid.Column="1" Text="3" Height="24" VerticalContentAlignment="Center" />
        </Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" IsDefault="True" Padding="18,4" Margin="0,0,8,0" Click="Ok_OnClick" />
            <Button Content="Cancelar" IsCancel="True" Padding="12,4" />
        </StackPanel>
    </StackPanel>
</Window>

